# Requirements for cross-document entity relationships module
# These dependencies should be added to the main project's requirements.txt

# Core dependencies (likely already in main project)
fastapi>=0.104.0
pydantic>=2.0.0
neo4j>=5.0.0

# Gemini 2.0 Flash support
google-generativeai>=0.8.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Additional utilities (if needed)
python-dotenv>=1.0.0  # For environment variable loading
